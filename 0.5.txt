1. GET request from browser to server and responds with 200
2. 3 GET requests for main.cs spa.js data.json
3. Note right of browser: The browser executes the callback function that renders the notes